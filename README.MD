Create a ML project folder in pc/laptop

Create environment in that folder
 To create we use python -m venv folder_name

Activate the environment
 To activate we use ./folder_name/scripts/activate

Create backend folder, frontend folder and Readme.md file

In backend we created data folder ,model folder,requirements.txt, app.py,pickle file, train_save.py

In frontend we created app.py and requirements.txt

#Inside data,we created a file load_data.py and in train_model created a file train_model.py

#After creating the files we created train_save.py and loaded the  requirements.txt

#To load libraries we use pip install -r requirements.txt

#After this we ran our train_save.py which created pkl file

#Run the app.py

In Frontend we created requirements.txt and app.py before we run we will load the libraries which are required in the frontend folder 

#After loading the libraries we ran the app.py in frontend through streamlit

#To run streamlit  we use streamlit run File_name.py command